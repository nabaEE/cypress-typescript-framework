pipeline {
  agent any

  tools {
    nodejs 'Node 18'
  }

  environment {
    CYPRESS_CACHE_FOLDER = "${WORKSPACE}/.cache/Cypress"
  }

  stages {
    stage('Clone GitHub repo') {
      steps {
        git branch: 'main',
            url: 'https://github.com/your-username/your-cypress-repo.git',
            credentialsId: 'github-creds'  // <-- Your Jenkins credential ID here
      }
    }

    stage('Install Dependencies') {
      steps {
        sh 'npm ci'
      }
    }

    stage('Run Cypress Tests') {
      steps {
        sh 'npx cypress run'
      }
    }
  }
}
